# Legacy CLI

This directory archives the original command-line interface, its tests, and a
copy of the former `backend/aa_logic.py`.

The active desktop application does not import from this package. New backend
work belongs in `backend/task_rules.py`, `backend/validation.py`, or the
appropriate service module.
